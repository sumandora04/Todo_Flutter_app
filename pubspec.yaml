name: todo_clear
description: A todo app.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^1.10.0
  firebase_auth: ^3.2.0
  cloud_firestore: ^3.1.0
  provider: ^6.0.1
  flutter_slidable: ^1.1.0 #Slidable for list items.
  swipe: ^0.0.1 #for swipe gesture.
  carousel_slider: ^4.0.0
  flutter_datetime_picker: ^1.5.1
  path: ^1.8.0
  path_provider: ^2.0.7
  sqflite: ^2.0.0+4
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
